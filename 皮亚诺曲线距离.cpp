#include<iostream>
#include<cstring>
#include<cmath>
#define x first
#define y second
using namespace std;
typedef long long ll;
typedef pair<ll,ll> PLL;
PLL calc( n,ll m)
{
    if(n==0) return {0,0};
    double cnt = pow(3,2*n);
    double 
}
